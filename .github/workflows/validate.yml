name: Validate

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  repository:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate source and package
        shell: pwsh
        run: pwsh -NoProfile -File build/Test-Repository.ps1

